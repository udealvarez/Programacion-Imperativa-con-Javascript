// Crear un array
let edades = [24, 37, 18, 59, 7];
let nombre = ["Juana", "Raul", "Alan"];
let boleanos = [true, false, false, true];
let combinados = [15, "hola", true];

// Aceder a un elemento especifico de un array
console.log(edades[3]);
console.log(nombre[0]);
console.log(boleanos[0]);


//Recorrer un array
for (let i = 0; i < edades.length; i++) { // en este caso LENGTH nos devuelve la cantidad de elementos de array  --> LENGTH es una propiedad
    console.log(edades[i]);
    
}

